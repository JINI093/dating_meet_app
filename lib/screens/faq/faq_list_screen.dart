import 'package:flutter/material.dart';

class FaqListScreen extends StatelessWidget {
  const FaqListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('FAQ 목록 화면'),
      ),
    );
  }
} 